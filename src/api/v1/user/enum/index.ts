export enum DeviceType {
  APPLE = '1',
  SAMSUNG = '2',
}

export enum UserType {
  ADMIN = 'admin',
  USER = 'user',
  GUEST = 'guest',
}

export enum UserStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
  BUSY = 'busy',
}
